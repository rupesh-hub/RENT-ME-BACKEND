
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rent_me_db
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format-sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: localhost
    port: 1025
    username: rupesh
    password: rupesh
    properties:
      mail:
        auth: true
        connectiontimeout: 5000
        timeout: 3000
        writetimeout: 5000
        starttls:
          enable: true
        smtp:
          true: "*"


